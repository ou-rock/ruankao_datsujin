[project]
name = "ruankao-agent"
version = "0.1.0"
description = "Local Codex companion agent for System Architecture Designer exam preparation"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
